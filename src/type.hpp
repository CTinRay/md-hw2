#ifndef TYPE_HPP
#define TYPE_HPP

typedef long double Real;
typedef unsigned int Index;

enum Label {
    positive = +1, negative = -1
};

enum FactorType {
    f, g, h
};
    
#define RealMulId 1
#define RealAddId 0

#endif
